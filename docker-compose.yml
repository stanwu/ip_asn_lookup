services:
  ip-asn-api:
    build: .
    container_name: ip-asn-api
    ports:
      - "8000:8000"
    environment:
      HOST: "0.0.0.0"
      PORT: "8000"
      RATE_LIMIT_REQUESTS: "60"
      RATE_LIMIT_WINDOW_SEC: "60"
    restart: unless-stopped
